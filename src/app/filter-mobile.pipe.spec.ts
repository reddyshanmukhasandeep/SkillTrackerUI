import { FilterMobilePipe } from './filter-mobile.pipe';

describe('FilterMobilePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMobilePipe();
    expect(pipe).toBeTruthy();
  });
});
